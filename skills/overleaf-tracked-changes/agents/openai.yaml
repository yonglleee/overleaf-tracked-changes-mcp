interface:
  display_name: "Overleaf Tracked Changes"
  short_description: "Snapshot Overleaf, then replay safe tracked edits"
  default_prompt: "Use $overleaf-tracked-changes to snapshot an Overleaf project and safely revise it with tracked changes."
